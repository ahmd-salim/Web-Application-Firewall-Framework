# Web-Application-Firewall-Framework
A lightweight Web Application Firewall (WAF) prototype built with FastAPI that acts as a reverse proxy between clients and a backend application.  The project demonstrates how a security layer can inspect, filter, and control incoming HTTP requests before they 
